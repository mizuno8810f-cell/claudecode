/**
 * Object positions in game.json are authored against a square virtual
 * canvas of this size; the UI converts them to percentages so the room
 * area scales responsively (this app targets iPhone-width screens).
 */
export const ROOM_CANVAS_SIZE = 400;
